pipeline {
    agent any  // Exécuter sur n'importe quel agent

    stages {
        stage('Build') {
            steps {
                echo 'Construire le projet'
                // Ajoutez des étapes pour construire votre projet ici
            }
        }
        stage('Test') {
            steps {
                echo 'Exécuter les tests'
                // Ajoutez des étapes pour exécuter les tests ici
            }
        }
        stage('Deploy') {
            steps {
                echo 'Déployer l\'application'
                // Ajoutez des étapes pour déployer votre application ici
            }
        }
    }
}
